{
  "name": "pi-planit",
  "version": "0.1.0",
  "description": "Chat based plan mode for Pi.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "author": "jmaliksi",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jmaliksi/pi-planit.git"
  },
  "keywords": ["pi-package"],
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/prompts/*.md dist/",
    "dev": "tsc --watch",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm run test",
    "prepare": "npm run build"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
